<!DOCTYPE html>
<html>

<head>
  <title>LoveAndBeer</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <header class="header container-fluid sticky-top">
    <div class="row">
      <div class="col-md-12 bg-beer">
        <div class="header-left-content float-left">
            <div class="header-logo text-white">
                <h1 class="header-logo">Love and Beer</h1>
            </div>
        </div>
        <div class="navigation flote-right" width="200">
          <nav>
            <ul>
              <% if logged_in? %>
                <ol class="float-right"><%= link_to "Logout", logout_path, class: "btn btn-secondary btn-default mr-5 mt-3 glyphicon glyphicon-log-out" %></ol>
                <ol class="float-right"><%= link_to "Post", new_post_path ,class: "glyphicon glyphicon-pencil btn btn-secondary btn-default mr-5 mt-3"%></ol>          
              <% end%>
            </ul>
          </nav>
        </div>
        </div>
      </div>
    </div>

  </header>
  <% flash.each do |message_type, message| %>
  <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>

  <%= yield %>

  <footer class="container-fluid fixed-bottom">
      <div class="row">
          <div class="col-12 text-center text-white bg-beer py-4">
              <p>Copyright © 2019 delta All rights reserved.</p>
          </div>
      </div>
  </footer>
</body>

</html>